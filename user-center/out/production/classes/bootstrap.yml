server:
  port: 8082

spring:
  application:
    name: server-user
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: server-config
      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/