server:
  port: 8780

spring:
  application:
    name: server-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ChineseLincoln/SpringcloudConfig/
          label: master
          searchPaths: respo
          username: sodlinken@gmail.com
          password: xiaoli901001

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

