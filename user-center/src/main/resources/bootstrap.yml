server:
  port: 8082

spring:
  application:
    name: user-center
#  cloud:
#    config:
#      discovery:
#        enabled: true
#        serviceId: server-config
#      profile: dev

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/