server:
  port: 8080

spring:
  application:
    name: server-gate

zuul:
  prefix: /api
#  routes:
#    user-center:
#      path: /uc/**
#      serviceId: user-center

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/